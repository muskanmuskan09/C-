#include<iostream>
#include<vector>
using namespace std;
int main(){
    int n;
    cout<<"Enter the value of n:";
    vector<int> number(3);
    number.insert(number.begin()+1, 50);
    cout << numbers.at(0) << endl;
cout << numbers.at(1) << endl;
cout << numbers.at(2) << endl;

    return 0;
}